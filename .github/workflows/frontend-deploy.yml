# name: Deploy Angular to S3 + CloudFront

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     defaults:
#       run:
#         # Sets the working directory for the run steps to your Angular app
#         working-directory: frontend/testi-frontend

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Use Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 22.12.0

#       - name: Install dependencies
#         run: npm ci

#       - name: Build Angular app
#         run: npm run build -- --configuration production

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: 'eu-north-1'

#       - name: Deploy to S3
#         run: |
#           aws s3 sync dist/testi-frontend/browser s3://${{ secrets.AWS_S3_BUCKET }} --delete

#       - name: Invalidate CloudFront cache
#         run: |
#           aws cloudfront create-invalidation \
#             --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
#             --paths "/*"
